# Constraints

This folder contains FPGA constraint files.

Depending on the FPGA platform, it may include:

- XDC files (Xilinx)
- SDC files (Intel)

If the project is used only for simulation, this folder may remain empty.